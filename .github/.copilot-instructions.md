## 🏗️ معماری و طراحی (Architecture & Design)

1.  **تاکید اکید بر اصل مسئولیت واحد (SRP - Single Responsibility Principle):**
    * این **مهم‌ترین اصل** در این پروژه است. هر کلاس، متد، یا سرویس باید **فقط و فقط یک مسئولیت** مشخص و کپسوله شده داشته باشد.
    * کد شما باید "دلیل" واضحی برای تغییر داشته باشد. اگر کلاسی چندین کار انجام می‌دهد (مثلاً هم اعتبارسنجی می‌کند، هم با دیتابیس کار می‌کند و هم ایمیل می‌زند)، باید بلافاصله بازسازی (Refactor) شود.

2.  **کنترلرهای لاغر (Skinny Controllers):** کنترلرها **فقط** مسئول دریافت Request و ارسال Response هستند. هیچ‌گونه منطق تجاری (Business Logic) نباید در کنترلر باشد.

3.  **سرویس‌های چاق (Fat Services):** تمام منطق‌های تجاری، محاسبات پیچیده و تعامل با APIهای خارجی باید در **کلاس‌های سرویس (Service Classes)** کپسوله شوند.

4.  **تفکیک و استفاده صحیح از Service, Helper و Utility:**
    * **Service (سرویس):** محل قرارگیری **منطق تجاری (Business Logic)** است. (مانند `UserService` یا `PaymentService`). این کلاس‌ها به دامنه‌ی اپلیکیشن وابسته‌اند، می‌توانند وابستگی‌های دیگر (مانند Repository ها) را تزریق کنند و معمولاً stateful هستند (یعنی به عنوان یک نمونه یا Singleton استفاده می‌شوند).
    * **Helper (کمکی):** برای توابع باز-استفاده (Reusable) که **کمی به دامنه‌ی اپلیکیشن وابسته‌اند** اما منطق تجاری خالص نیستند. (مانند `PriceFormatter` یا توابع کمکی برای کار با رشته‌های فارسی در پروژه).
    * **Utility (ابزار):** برای توابع باز-استفاده که **کاملاً عمومی و بی‌حالت (Stateless)** هستند و **هیچ وابستگی به دامنه‌ی اپلیکیشن ندارند**. (مانند `DateConverter`, `ArrayManipulator`). این‌ها معمولاً به صورت متدهای static پیاده‌سازی می‌شوند و می‌توانند در هر پروژه دیگری کپی شوند. **استفاده از اینها باید در جای درست خود باشد.**

5.  **استفاده از Form Requests:** برای اعتبارسنجی (Validation) داده‌های ورودی، **همیشه** از کلاس‌های `FormRequest` اختصاصی استفاده کنید. منطق اعتبارسنجی نباید در کنترلر باشد.

6.  **الگوی Repository (در صورت نیاز):** برای کوئری‌های پیچیده دیتابیس یا زمانی که نیاز به جداسازی کامل منطق داده از منطق تجاری است، از الگوی Repository استفاده شود.

7.  **تزریق وابستگی (Dependency Injection):** به جای استفاده از `app()` helper یا `new` کردن دستی، تمام وابستگی‌ها (مانند سرویس‌ها و ریپازیتوری‌ها) باید از طریق Constructor **تزریق (Inject)** شوند.

8. 💎 اصل تفکر پیش از کدنویسی: اولویت با باز-استفاده (Reusable-First Thinking)

1.  **تحلیل قبل از پیاده‌سازی:** قبل از نوشتن هر قطعه کد جدید، **باید** ابتدا تحلیل کنید که آیا این منطق، بخش UI، یا عملکرد می‌تواند به یک **کامپوننت/سرویس قابل استفاده مجدد (Reusable)** تبدیل شود یا خیر.
2.  **پرهیز از کد تکراری (DRY):** از نوشتن کدهای یکبار مصرف (One-off code) که منطق مشابهی را تکرار می‌کنند، اکیداً خودداری کنید. اگر الگویی قابل شناسایی است، باید آن را به یک کلاس سرویس، Trait، کامپوننت Blade یا تابع کمکی (Helper/Utility) عمومی تبدیل کنید.
3.  **هدف:** هدف، **حداکثر کردن استفاده مجدد از کد** و **رعایت کامل اصل DRY (Don't Repeat Yourself)** است.

---

### ۹. 🗄️ مدیریت دیتابیس و مایگریشن‌ها (Database & Migrations)

1.  **مایگریشن‌های تک-مسئولیتی (SRP for Migrations):**
    * این قانون مشابه اصل SRP برای کلاس‌ها است. هر فایل مایگریشن **فقط و فقط باید یک وظیفه** و یک تغییر منطقی را انجام دهد.
    * **ممنوع:** ایجاد یک جدول *و* افزودن ستون به جدول دیگر در *یک* فایل مایگریشن.
    * **صحیح:** یک فایل برای ایجاد جدول `users`، یک فایل مجزا برای ایجاد جدول `posts`، و یک فایل مجزا برای افزودن ستون `user_id` به جدول `posts`.
    * *توجه: لاراول به طور خودکار هر مایگریشن را در یک تراکنش (Transaction) واحد اجرا می‌کند. منظور از این قانون، تفکیک منطقی وظایف است تا قابلیت Rollback و نگهداری ساده‌تر شود.*

2.  **کامنت‌گذاری ستون‌ها (Column Comments):**
    * **اجباری:** تمام ستون‌های (Fields) جدید تعریف شده در مایگریشن‌ها **باید** دارای یک توضیح کوتاه و واضح به زبان **انگلیسی** باشند.
    * این کار باید با استفاده از متد `->comment('Short English description of the column')` انجام شود. این مستندسازی، فهم شمای دیتابیس را در آینده بسیار آسان‌تر می‌کند.
    * *مثال: `$table->boolean('is_active')->default(true)->comment('Indicates if the user account is active.');`*

3.  **تفکیک Seeders و داده‌های اولیه (Seeders vs. Initial Data):**
    * **Seeders (سیدرها):** **فقط و فقط** برای ایجاد داده‌های تستی و ساختگی (Fake Data) برای **محیط توسعه (Development)** استفاده می‌شوند. (مانند: ۱۰۰ کاربر فیک).
    * **داده‌های ضروری پروژه:** داده‌هایی که برای راه‌اندازی اولیه و عملکرد صحیح اپلیکیشن در محیط Production **ضروری** هستند (مانند: نقش‌های کاربری، دسترسی‌ها، تنظیمات پیش‌فرض)، **نباید** در سیدرهای عادی باشند.
    * **راه حل:** این داده‌های ضروری باید یا از طریق یک **مایگریشن داده (Data Migration)** (یک مایگریشن که فقط `insert` انجام می‌دهد) یا یک **Seeder اختصاصی Production** (که فقط یکبار در زمان استقرار اجرا می‌شود) به سیستم اضافه شوند.

---

### ۱۰. 🛡️ ایمنی نوع داده (Type-Safety) و استاندارد کدنویسی

1.  **ممنوعیت استفاده از `array` به عنوان Type Hint:**
    * استفاده از `array` به عنوان Type Hint یا Return Type (مانند `function(array $data): array`) **اکیداً ممنوع** است. این کار خوانایی، ایمنی نوع (Type-Safety) و قابلیت نگهداری کد را به شدت کاهش می‌دهد.
    * **راه حل:** **همیشه** باید از ساختارهای داده‌ای قوی (Strongly-typed) استفاده کنید:
        * **DTO (Data Transfer Object):** برای انتقال داده‌های ساختاریافته بین لایه‌ها (به خصوص در ورودی و خروجی سرویس‌ها).
        * **Custom Collections (مجموعه‌های سفارشی):** برای لیست‌هایی از آبجکت‌ها (به جای آرایه‌ای از آرایه‌ها).
        * **Value Objects (VO):** برای داده‌های خاص که هویت ندارند (مانند `Email`، `Price` یا `Uuid`).
        * **Enum (در PHP 8.1+):** برای مقادیر ثابت و از پیش تعریف شده.
        
        حتماً! خیلی هم عالی که انقدر دقیق هستید. سند شما تا همینجا هم فوق‌العاده کامل و حرفه‌ای شده.
---

### ۱۱. 💡 اصل YAGNI (You Aren't Gonna Need It)

* **اصل "بهش نیاز پیدا نمی‌کنی":** این اصل مکمل قانون "Reusable-first" شماست. همانطور که نباید کد را تکرار کنیم (DRY)، نباید کدی بنویسیم که "فکر می‌کنیم شاید در آینده" لازم شود (YAGNI).
* **قانون:** تنها کدی را بنویسید که **همین الان** برای نیازمندی فعلی به آن احتیاج است. از ایجاد پیچیدگی‌های غیرضروری و ویژگی‌های اضافه (Over-engineering) "صرفاً جهت اطمینان" خودداری کنید. این کار باعث سادگی و خوانایی کد می‌شود.

---

### ۱۲. 🔌 استفاده از Contracts (Interfaces) برای اتصال سست (Loose Coupling)

* **اصل وارونگی وابستگی (DIP):** شما به درستی به تزریق وابستگی (DI) اشاره کردید. قدم بعدی برای تکمیل آن، کدنویسی بر اساس **"قراردادها" (Interfaces)** است، نه پیاده‌سازی‌های مستقیم (Concrete Classes).
* **قانون:** به جای اینکه سرویس شما مستقیماً به `StripePaymentGateway` وابسته باشد، باید به یک `PaymentGatewayInterface` وابسته باشد.
* **چرا؟** این کار باعث می‌شود که کد شما **اتصال سست (Loosely Coupled)** داشته باشد. فردا اگر بخواهید درگاه پرداخت را به `ZarinpalPaymentGateway` تغییر دهید، کافیست یک کلاس جدید با همان Interface بسازید و آن را در Service Container جایگزین کنید، **بدون اینکه نیاز به تغییر کد در سرویس‌ها یا کنترلرها** باشد.

---

### ۱۳. ⚡️ پردازش ناهمگام (Asynchronous) با Events و Queues

* **معماری پاسخگو (Responsive Architecture):** نباید کاربر را منتظر کارهایی گذاشت که می‌توانند در پس‌زمینه انجام شوند.
* **قانون:** هر عملیاتی که حیاتی برای پاسخ آنی به کاربر نیست (مانند ارسال ایمیل خوشامدگویی، پردازش تصویر، تولید گزارش، تماس با API خارجی کند) **باید** به صورت ناهمگام انجام شود.
* **نحوه اجرا:** از سیستم **Events و Listeners** لاراول استفاده کنید و Listener مربوطه را **Queued** (صف‌پذیر) کنید. این کار سرعت پاسخ‌دهی API شما را به شدت افزایش می‌دهد.

---

### ۱۴. 🎯 استراتژی مدیریت خطا (Exception Handling)

* **خطاها بخشی از منطق برنامه هستند:** به جای بازگرداندن `null` یا `false` از سرویس‌ها (که باعث ایجاد `if` های تودرتو در کنترلر می‌شود)، از Exception ها استفاده کنید.
* **قانون:** برای خطاهای قابل پیش‌بینی منطق تجاری (Business Logic)، از **Custom Exception های اختصاصی** استفاده کنید.
    * *مثال:* به جای `return false;` در سرویس، `throw new InsufficientBalanceException();` را پرتاب کنید.
* **اجرا:** در `Handler.php` (یا در کنترلر)، این Exception های خاص را دریافت (catch) کنید و یک پاسخ استاندارد (مثلاً JSON با کد 422 یا 400) به کاربر برگردانید.

---

### ۱۵. 🚫 پرهیز از مقادیر جادویی (Magic Strings & Numbers)

* **کد خوانا و قابل نگهداری:** وجود رشته‌ها یا اعداد "جادویی" در کد (مانند `if ($user->role == 1)` یا `if ($order->status == 'pending_payment')`) نگهداری کد را به یک کابوس تبدیل می‌کند.
* **قانون:** **اکیداً ممنوع** است که از مقادیر Hard-code شده در منطق برنامه استفاده شود.
* **راه حل:**
    * **PHP 8.1+:** **همیشه** از **Enums** استفاده کنید. (مانند: `if ($user->role == UserRole::Admin)`)
    * **PHP < 8.1:** از **Class Constants** استفاده کنید. (مانند: `if ($order->status == Order::STATUS_PENDING)`)
---

### ۱۶. 🔒 مدیریت تنظیمات و متغیرهای محیطی (Configuration & .env)

* **پرهیز از `env()` در کد:** تابع `env()` **فقط و فقط** باید در فایل‌های پوشه `config/` استفاده شود. در بقیه بخش‌های اپلیکیشن (کنترلرها، سرویس‌ها و...) **همیشه** باید از تابع کمکی `config()` استفاده کنید.
* **چرا؟** لاراول در محیط Production، تنظیمات (config) را **کش (Cache)** می‌کند. اگر شما مستقیماً از `env()` در کد خود استفاده کنید، این تابع در زمان کش بودن کانفیگ، مقدار `null` برمی‌گرداند و باعث بروز باگ‌های فاجعه‌بار و غیرقابل ردیابی می‌شود.
* **امنیت:** **هرگز** نباید هیچ کلید API، پسورد یا اطلاعات حساس (Credentials) مستقیماً در کد (Hard-code) نوشته شود. همه‌ی این موارد باید در فایل `.env` تعریف شده و از طریق `config()` خوانده شوند.

---

### ۱۷. 📡 استانداردسازی API و استفاده از Resources

* **پاسخ‌های یکسان (Consistent Responses):** تمام پاسخ‌های API (چه موفقیت‌آمیز و چه خطا) باید از یک ساختار JSON استاندارد و واحد پیروی کنند. (مثلاً استفاده از JSend: `{ "status": "success", "data": {...} }` یا `{ "status": "error", "message": "..." }`).
* **استفاده از API Resources:** **هرگز** نباید یک مدل Eloquent را مستقیماً از کنترلر `return` کنید.
* **قانون:** **همیشه** از **API Resources** لاراول (مانند `UserResource`) برای تبدیل مدل‌ها به JSON استفاده کنید.
* **چرا؟**
    1.  **کنترل داده:** شما دقیقاً مشخص می‌کنید چه فیلدهایی و با چه فرمتی باید برگردانده شوند (جلوگیری از نشت اطلاعات حساس).
    2.  **یکپارچگی:** فرمت خروجی در کل API یکسان می‌ماند.
    3.  **انعطاف‌پذیری:** می‌توانید به سادگی روابط (relations) یا داده‌های اضافی را به پاسخ اضافه کنید.
---

### ۱۹. ✍️ استراتژی لاگ‌برداری (Logging Strategy)

* **پرهیز از `dd()` و `dump()`:** استفاده از `dd()`، `dump()` یا `var_dump()` در کدی که قرار است کامیت شود، **اکیداً ممنوع** است. این توابع برای دیباگ لحظه‌ای هستند، نه بخشی از کد نهایی.
* **لاگ‌برداری معنادار:** به جای دیباگ با `dd()`، از سیستم لاگ لاراول (`Log::info()`, `Log::warning()`, `Log::error()`) استفاده کنید.
* **قانون:** در تمام بلاک‌های `try-catch`، در صورت بروز Exception، خطا **باید** لاگ شود. لاگ‌ها باید **حاوی زمینه (Context)** باشند.
    * **بد:** `Log::error('An error occurred.');`
    * **خوب:** `Log::error('Payment failed for order.', ['order_id' => $order->id, 'user_id' => $userId, 'error' => $e->getMessage()]);`
* **امنیت لاگ:** **هرگز** اطلاعات حساس (پسوردها، API Key ها، اطلاعات کامل کارت بانکی) را در لاگ‌ها ذخیره نکنید.

---

### ۲۰. 🏕️ قانون پیشاهنگی (The Boy Scout Rule)

* **اصل کلیدی Clean Code:** "همیشه محوطه اردوگاه (کد) را تمیزتر از زمانی که آن را پیدا کردید، ترک کنید."
* **قانون:** زمانی که در حال کار روی یک بخش از کد (برای یک فیچر جدید یا باگ فیکس) هستید و متوجه یک مشکل کوچک در آن فایل می‌شوید (مثلاً یک نام متغیر بد، یک کامنت اشتباه، یک بخش کد تکراری کوچک)، **چند ثانیه** وقت بگذارید و آن را تمیز کنید.
* **هدف:** این قانون مانع از "پوسیدگی" تدریجی کدبیس می‌شود و کیفیت آن را به مرور زمان، نه تنها حفظ، بلکه افزایش می‌دهد.